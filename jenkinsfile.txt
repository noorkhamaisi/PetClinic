pipeline {
    agent { label 'firstSlave' }
    stages {
        stage('build') {
            steps {
               git clone https://github.com/spring-projects/spring-petclinic.git
		cd spring-petclinic
		./mvnw package
            }
        }
    }
}